apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: snack-rating-sqlite-pvc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi # small db — adjust up if you expect real growth
